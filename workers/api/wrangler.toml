name = "mindbridge-api"
main = "src/index.ts"
compatibility_date = "2024-09-01"

# Set ALLOW_ORIGIN to your deployed PWA origin (Cloudflare Pages URL or custom domain).
[vars]
ALLOW_ORIGIN = "https://mindbridge.pages.dev"

# Secrets (do NOT put real values here — use: wrangler secret put NAME)
#   wrangler secret put PAYSTACK_SECRET_KEY
#   wrangler secret put CF_TURN_KEY_ID
#   wrangler secret put CF_TURN_API_TOKEN
